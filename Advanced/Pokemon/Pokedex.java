public class Pokedex extends PokemonAbstract {
	public void pokemonInfo(Pokemon pokemon) {
	     System.out.println("Info: Name is " + pokemon.getName() + ". Health is " + pokemon.getHealth() + ". Type is "+ pokemon.getType());
	}
	
}
